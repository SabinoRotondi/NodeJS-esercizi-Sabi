const sum = () => console.log(2+7)
export default sum;

//used mjs to let node know we are working with ECMAScript modules, we could leave the js extension modifying the package.json: ("type" : "module")